//aca solo van definiciones, interface, types..
export interface WelcomeProps {
    name: string;
}

interface User {
    nick: string
    salary: number
    avatar: string
    description?: string
  
  }